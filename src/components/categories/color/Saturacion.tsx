import Button from "../../Button";

const Saturacion = () => {
  return (
    <Button
      image={require("../../../assets/img/saturacion.png")}
      name="Saturacion"
    />
  );
};

export default Saturacion;
