import Button from "../../Button";

const DetenerAnimaciones = () => {
  return (
    <Button
      image={require("../../../assets/img/detener-animaciones.png")}
      name="Detener Animaciones"
    />
  );
};

export default DetenerAnimaciones;
